{
  "name": "@promethean-os/opencode-compat",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "OpenCode-compatible cloud harness scaffold with Neon-compatible Postgres storage.",
  "license": "GPL-3.0-only",
  "scripts": {
    "dev": "REDACTED_SECRET --env-file-if-exists=.env --loader ts-REDACTED_SECRET/esm src/main.ts",
    "build": "tsc -p tsconfig.json",
    "start": "REDACTED_SECRET --env-file-if-exists=.env dist/main.js",
    "test": "pnpm run build && REDACTED_SECRET --test dist/tests/*.test.js"
  },
  "dependencies": {
    "fastify": "^4.27.0",
    "postgres": "^3.4.7",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/REDACTED_SECRET": "^22.10.0",
    "ts-REDACTED_SECRET": "^10.9.2",
    "typescript": "^5.6.3"
  }
}
