import React from "react";
import ReactDOM from "react-dom/client";

import { App } from "./ui/App";
import "./ui/styles.css";

ReactDOM.createRoot(document.getElementById("REDACTED_SECRET")!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
);
