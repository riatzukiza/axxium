
gemma4 called tools in manual mode, not a problem there...

for some reason it just renders really screwed up on the frontend when I make a request to a manual agent, it was able to call tools though. And it was able to answer a question about the code base.

know what man, that file has to go.

it's one of those things I keep telling agents I prioritize.

taht the "event agents" have to go.

we have "events", which come IN from outside of cljs. So like, discord message, a scheduled event,

we have triggers.

They listen for events, and execute an action, if a condition is met.

so, we get rid of "manual" or "discord" and we get rid of the "source" concept. from agents.

so no :trigger-kind :source-kind :sources :source-mode :cadance

no :source
  {:max-messages 8
   :max-total-messages 40
   :max-message-chars 1200
   ;; :stickySession true
   :sessionMaxMessages 240} 
